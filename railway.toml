[build]
builder = "nixpacks"
buildCommand = "pnpm install && cd packages/shared && pnpm run build && cd ../../services/api && npx nest build"

[deploy]
startCommand = "cd services/api && node dist/main.js"
