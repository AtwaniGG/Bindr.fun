[build]
builder = "nixpacks"
buildCommand = "pnpm install && cd services/api && npx prisma generate && cd ../../packages/shared && pnpm run build && cd ../../services/api && npx nest build"

[deploy]
startCommand = "cd services/api && npx prisma migrate deploy && node dist/main.js"
